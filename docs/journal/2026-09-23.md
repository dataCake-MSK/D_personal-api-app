# 2026-09-23 일간 일지

## 요약
- #7(시계열 그래프)을 머지해 **M1 MVP를 완료**했다. PRD 성공 기준을 실기기 확인 결과로 점검하고 완료 리포트를 작성했다.

## 지표
| 커밋 | 머지된 PR | 닫힌 이슈 | 열린 이슈 |
|---|---|---|---|
| 2 (이 PR) | 1 (#33) | 1 (#7) | 0 |

## 주요 활동
- PR #33 머지 → M0 2개, M1 11개 이슈 전부 종료
- PRD 성공 기준 점검: 텍스트·테이블·그래프·전송 모두 실기기 확인 완료
- 완료 리포트 작성(`docs/reports/2026-09-23-m1-completion.md`)
- PRD에 달성 표기, PRD-006은 부분 달성(헤더 입력 UI 없음)으로 명시

## 완료한 이슈 / PR
- PR #33 → #7 닫힘

## 결정 사항
- PRD-006은 부분 달성으로 기록. 잔여분은 NB-007(헤더 입력 UI)로 추적

## 막힌 점 / 이슈
- 패키지 설치 후 개발 서버를 재시작하지 않아 폰에서 모듈을 찾지 못함 → 설치 후 재시작 필요
- 차트 애니메이션 타이머가 다른 테스트를 깨뜨림(CI만 실패) → 테스트에서 차트 대체

## 배운 점
- [expo-go-remote-preview.md](../learning/expo-go-remote-preview.md)

## 다음 계획
- NB-007(헤더 입력 UI) 승격 → M2 범위 결정
- 자동 갱신 주기, export/import, 배포(EAS) 검토

---

## 개발자 퀵 가이드
> 이날 결과로 이어진 진행사항만 압축. 시행착오는 제외(→ 막힌 점, 학습 노트).

### 현재 상태 (M1 완료 시점)
- 위젯 3종: 텍스트 / API 데이터(텍스트·테이블·그래프) / 데이터 전송
- 화면 2개: 대시보드(`src/app/index.tsx`), API 키 관리(`src/app/secrets.tsx`)
- 소스 22개 파일, 테스트 75개, CI 약 40~55초

### 핵심 명령
```bash
npm ci
npx expo start --tunnel --go          # 폰 Expo Go로 확인 (패키지 설치 후에는 반드시 재시작)
npm run lint && npm run format:check && npm run typecheck && npm test
```

### 새 위젯을 추가하려면
1. `src/widgets/<이름>/<이름>-widget.tsx`에 정의 작성
   - `configSchema`(zod), `Renderer`, `ConfigEditor`, `defaultConfig`
2. `src/widgets/index.ts`의 `registerBuiltInWidgets()`에 `registerWidget(...)` 한 줄 추가
3. 네트워크가 필요하면 `src/lib/http-request.ts`의 `requestJson`(조회) 또는 `runAction`(전송) 사용 — 비밀 값 치환·주소 검사가 이미 들어 있다
4. 테스트는 `__tests__/`에 추가하고 `npm test`로 확인
