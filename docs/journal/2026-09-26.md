# 2026-09-26 일간 일지

## 요약
- M2 범위 문서(#43)와 헤더 입력 UI(#44)를 머지했다. 위젯 구조(조회/전송 구분, 새로고침, 주기 갱신)를 검토해 리포트로 정리했다.

## 지표
| 커밋 | 머지된 PR | 닫힌 이슈 | 열린 이슈 |
|---|---|---|---|
| 1 (이 PR) | 2 (#43, #44) | 1 (#35) | 7 |

## 주요 활동
- PR #43 머지: M2 범위(NB-008~014 → PRD-007~013 → SRS-018~025), 결정 D5~D8
- PR #44 머지: 요청 헤더 입력 UI → 인증 API 연결 확인(httpbin `/bearer`), PRD-006 완성
- 위젯 구조 검토 리포트 작성: 두 위젯을 합치지 않고 유지 + 새로고침·주기 갱신·이름 정리 제안
- D9~D12 확정: 2종 유지, 이름 "API 조회 (GET)"/"API 실행 (보내기)", 자동 갱신 기본 수동, 주기 30초~15분
- SRS-026~028 작성과 이슈 #46~#48 등록

## 완료한 이슈 / PR
- PR #43, PR #44 → #35 닫힘

## 결정 사항
- D9 위젯 2종 유지 / D10 이름에 GET 표기 / D11 자동 갱신 기본 수동 / D12 주기 30초·1분·5분·15분·수동

## 막힌 점 / 이슈
- 없음

## 배운 점
- TanStack Query의 주기 갱신은 화면에 보이는 동안만 동작하고, React Native에서는 앱 활성화 감지를 따로 연결해야 한다

## 다음 계획
- #36·#37(표시 옵션·글자 크기) → #46(새로고침) → #47(자동 갱신) → #48(이름·형식 우선 선택)

---

## 개발자 퀵 가이드
> 이날 결과로 이어진 진행사항만 압축. 시행착오는 제외(→ 막힌 점, 학습 노트).

### 핵심 진행사항
1. 헤더 입력 UI 완료: `src/widgets/common/headers-editor.tsx`를 두 위젯 설정에서 공용으로 사용. 저장 형태는 `{ 이름: 값 }` 그대로라 기존 치환 로직 재사용
2. 위젯 구조 검토: 조회(자동·읽기)와 전송(수동·부작용)은 되돌릴 수 있는지가 달라 분리 유지

### 핵심 코드
```tsx
// 위젯 설정에서 헤더 편집기 붙이는 방법
<HeadersEditor
  headers={value.headers as Record<string, string> | undefined}
  onChange={(headers) => onChange({ ...value, headers })}
/>
```
- 인증 확인용 주소: `https://httpbin.org/bearer` + 헤더 `Authorization: Bearer {{secret:이름}}`, 값 경로 `token`
