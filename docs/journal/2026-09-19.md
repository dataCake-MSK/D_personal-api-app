# 2026-09-19 일간 일지

## 요약
- 루프로 만든 #3·#5 PR을 모두 머지했다(#17은 의존성 목록 충돌 해결 후). 의존성 추가만 있는 PR은 AI가 머지하도록 머지 등급을 조정했다.

## 지표
| 커밋 | 머지된 PR | 닫힌 이슈 | 열린 이슈 |
|---|---|---|---|
| 1 머지 해결 + 이 일지 PR | 2 (#16, #17) | 2 (#3, #5) | 6 |

## 주요 활동
- PR #16(#3 대시보드 상태 저장소) 사용자 머지
- PR #17(#5 위젯 레지스트리)이 `package.json` 의존성 목록 충돌로 머지 불가 → main을 합쳐 zod·zustand 모두 유지, lock 파일 재생성, 전체 검증·CI 통과 후 머지
- 머지 등급 조정: 의존성 추가·마이너 업데이트, 내용 변경 없는 충돌 해결 → 🟢
- Expo Go 실기기 확인 시점 제안(#4부터)

## 완료한 이슈 / PR
- PR #16 → #3 닫힘
- PR #17 → #5 닫힘

## 결정 사항
- 머지 등급 🟢에 의존성 추가·마이너 업데이트, 충돌 해결 추가. 🟡에는 의존성 삭제·메이저 업그레이드만 남김

## 막힌 점 / 이슈
- 병렬로 만든 두 PR이 같은 파일의 같은 위치(의존성 목록)를 수정해 나중 PR이 충돌

## 배운 점
- [git-branch-merge-basics.md](../learning/git-branch-merge-basics.md) (충돌 해결 추가)

## 다음 계획
- #4 대시보드 화면 → Expo Go로 첫 실기기 확인

---

## 개발자 퀵 가이드
> 이날 결과로 이어진 진행사항만 압축. 시행착오는 제외(→ 막힌 점, 학습 노트).

### 핵심 진행사항
1. **#17 충돌 해결**: 작업 브랜치에 main 머지 → `package.json`은 양쪽 의존성 유지 → lock 파일은 main 쪽을 기준으로 `npm install`로 재생성 → 검증 → push → CI 통과 → 머지
2. **머지 등급 조정**: `CLAUDE.md`, 플레이북 리포트

### 핵심 명령
```bash
gh pr view 17 --json mergeable,mergeStateStatus     # CONFLICTING 확인
git switch feat/SRS-012-widget-registry && git pull
git merge main                                     # 충돌 파일 확인
# package.json: 충돌 표시 제거 후 양쪽 의존성 모두 남김
git checkout --theirs package-lock.json            # main 쪽 lock을 기준으로
npm install                                        # 합쳐진 package.json 기준으로 lock 재생성
npm run lint && npm run format:check && npm run typecheck && npm test
git add package.json package-lock.json && git commit && git push
gh pr checks 17 --watch && gh pr merge 17 --merge --delete-branch
```
